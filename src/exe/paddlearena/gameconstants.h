#pragma once

#include <sputter/math/fixedpoint.h>

const sputter::math::FixedPoint kGameConstantsBallSpeed = sputter::math::FixedPoint(300);
const sputter::math::FixedPoint kGameConstantsPaddleSpeed = sputter::math::FixedPoint(400);